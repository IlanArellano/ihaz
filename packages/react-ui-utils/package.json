{
  "name": "@ihaz/react-ui-utils",
  "version": "1.4.0-beta",
  "type": "module",
  "engines": {
    "node": ">=16"
  },
  "peerDependencies": {
    "react": ">=16"
  },
  "scripts": {
    "build": "set NODE_ENV=production&& set INPUT_DIR=src/&& set OUTPUT_DIR=lib/&& rollup -c && gulp copy-files",
    "test": "jest --watchAll"
  }
}
